apiVersion: v1
kind: Service
metadata:
  name: hpa-demo-service
  namespace: hpa-lab-01
spec:
  type: ClusterIP
  selector:
    app: hpa-demo
  ports:
    - name: http
      port: 80
      targetPort: http
