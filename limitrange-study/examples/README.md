Examples

These manifests are for reading and comparison only.

Do not apply them during the lab.

01-container-defaults.yaml

Shows default CPU and memory requests and limits for Containers.

02-container-min-max-ratio.yaml

Shows minimum values, maximum values, and maximum limit-to-request ratios.

03-pvc-storage-range.yaml

Shows a storage request range for PersistentVolumeClaims.
