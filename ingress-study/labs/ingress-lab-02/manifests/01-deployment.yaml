apiVersion: apps/v1
kind: Deployment
metadata:
  name: routing-demo
  namespace: ingress-routing-lab
spec:
  replicas: 2
  selector:
    matchLabels:
      app: routing-demo
  template:
    metadata:
      labels:
        app: routing-demo
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - name: http
              containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
