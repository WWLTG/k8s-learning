apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: ingress-lab-04
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      initContainers:
        - name: create-page
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              mkdir -p /work/web
              printf 'web pod: %s\n' "$POD_NAME" > /work/web/index.html
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          volumeMounts:
            - name: content
              mountPath: /work
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - name: http
              containerPort: 80
          readinessProbe:
            httpGet:
              path: /web/
              port: http
            initialDelaySeconds: 2
            periodSeconds: 3
          volumeMounts:
            - name: content
              mountPath: /usr/share/nginx/html
      volumes:
        - name: content
          emptyDir: {}
