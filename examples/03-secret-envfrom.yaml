apiVersion: apps/v1
kind: Deployment
metadata:
  name: secret-envfrom-demo
  namespace: secret-example
spec:
  replicas: 1
  selector:
    matchLabels:
      app: secret-envfrom-demo
  template:
    metadata:
      labels:
        app: secret-envfrom-demo
    spec:
      containers:
        - name: app
          image: busybox:1.36
          command: ["sh", "-c", "echo Secret keys imported; sleep 3600"]
          envFrom:
            - secretRef:
                name: app-secret
