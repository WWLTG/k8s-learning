apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: service-lab-02
spec:
  type: ClusterIP
  selector:
    app: api
    tier: backends
  ports:
    - name: http
      port: 8080
      targetPort: web
